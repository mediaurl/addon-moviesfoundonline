{
    "name": "addon-moviesfoundonline",
    "version": "1.0.0",
    "description": "",
    "main": "dist",
    "scripts": {
        "build": "tsc",
        "start": "watched-sdk start --prod dist",
        "develop": "watched-sdk start",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@types/cheerio": "^0.22.15",
        "husky": "^3.1.0",
        "lint-staged": "^9.5.0",
        "prettier": "^1.19.1",
        "tslint": "^5.20.1",
        "typescript": "^3.7.4"
    },
    "dependencies": {
        "@watchedcom/sdk": "latest",
        "cheerio": "^1.0.0-rc.3"
    },
    "husky": {
        "hooks": {
            "pre-commit": "lint-staged"
        }
    },
    "lint-staged": {
        "*.{js,ts,tsx,css,md}": [
            "tslint --fix",
            "prettier --write",
            "git add"
        ]
    }
}
