{
    "name": "addon-moviesfoundonline",
    "version": "1.0.0",
    "description": "",
    "main": "dist",
    "scripts": {
        "build": "tsc",
        "start": "watched-sdk start --single --prod",
        "develop": "watched-sdk start --single",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@types/cheerio": "^0.22.16",
        "husky": "^4.2.3",
        "lint-staged": "^10.0.7",
        "prettier": "^1.19.1",
        "tslint": "^6.0.0",
        "typescript": "^3.7.5"
    },
    "dependencies": {
        "@watchedcom/sdk": "^0.13.1",
        "cheerio": "^1.0.0-rc.3"
    },
    "husky": {
        "hooks": {
            "pre-commit": "lint-staged"
        }
    },
    "lint-staged": {
        "*.{js,ts,tsx,css,md}": [
            "tslint --fix",
            "prettier --write",
            "git add"
        ]
    }
}
